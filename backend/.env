# Copy this file to ".env" in the same folder (backend/) and fill in your
# real MySQL credentials. The .env file itself should never be committed
# to version control.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=10109810elias
DB_NAME=telco_churn_db

# Path to the trained pipeline, relative to backend/
MODEL_PATH=../model/churn_pipeline.pkl
