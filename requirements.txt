# Install with: pip install -r requirements.txt

# --- Model training ---
pandas
scikit-learn
joblib

# --- Backend (FastAPI + MySQL) ---
fastapi
uvicorn[standard]
python-dotenv
mysql-connector-python
pydantic

# --- Frontend (Streamlit) ---
streamlit
requests
